{% extends 'base-back.html.twig' %}

{% block title %}Edit Food
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}
{% block body %}
	{{ parent() }}


	<div class="card" style="width: 50rem; margin-left: 370px; margin-top: 100px">


		<div class="card-body">
			<h5 class="card-title">Edit Food</h5>

			{{ include('food/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('app_food_index') }}">back to list</a>

			{{ include('food/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
