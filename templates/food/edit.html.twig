{% extends 'base-back.html.twig' %}

{% block title %}Edit Food
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block body %}
	{{ parent() }}

	<div class="card" style="width: 50rem; margin-left: 370px; margin-top: 100px">
		<div class="card-body">

			<a href="{{ path('app_food_index') }}">
				<i class="fas fa-arrow-left"></i>
			</a>

			<br>
			<h5 class="card-title text-center">Edit Food</h5>

			{{ include('food/_form.html.twig', {'button_label': 'Update'}) }}


			
		</div>
	</div>
{% endblock %}
