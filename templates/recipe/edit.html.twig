{% extends 'base-back.html.twig' %}

{% block title %}Edit Recipe
{% endblock %}
{% block stylesheets %}
	{{ parent() }}
{% endblock %}
{% block body %}
	{{ parent() }}


	<div class="card" style="width: 50rem; margin-left: 370px; margin-top: 100px">


		<div class="card-body">
			<h5 class="card-title">Edit recipe
			</h5>

			{{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('app_recipe_index') }}">back to list</a>

			{{ include('recipe/_delete_form.html.twig') }}
		{% endblock %}
